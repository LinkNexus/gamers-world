<div {{ attributes.defaults({ class: 'input-with-icon' }) }}>
    {% block content %}{% endblock %}
    <twig:ux:icon
            name="noto-v1:game-die"
            data-action="click->forms--random-input#generatePassword"
            title="Generate a random password"
            class="clickable"
    />
</div>