import { GameProps } from "@/react/types";

export default function ({ urls, gameType }: GameProps) {
    return (
        <div>                
            <h1>Game is ready</h1>
        </div>
    );
}